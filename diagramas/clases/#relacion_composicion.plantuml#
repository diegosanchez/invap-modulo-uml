n@startuml

class Dispositivo {
        - ID : uint
 
        + Dispositivo(ID : uint, ruta : std::string)
        + ~Dispositivo()
        # std::vector<std::vector<char>> Sensar(std::vector<std::vector<char> > &v)
}

/'
 ' Dispositivo *-- basic_ifstream: "\n\n\n\n[reference]\n- puerto"
 '/

@enduml
